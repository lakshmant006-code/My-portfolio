AppHeader — the soft-green brand band at the top of app screens.

```jsx
<AppHeader logoSrc="../../assets/logo-mark.png" />
```

Solid `--green-300` band (default 96px tall) with the Hiku mountain mark bottom-left. Pass `align="center"` or custom `children` to override. Sits directly above the search bar on Explore/Trails.
