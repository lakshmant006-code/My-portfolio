MapMarker — a circular POI pin overlaid on the Trails map.

```jsx
<MapMarker icon="tent" />
<MapMarker icon="map-pin" active />
```

White floating disc with a green icon; `active` inverts to a deep-green fill with a white icon. Absolutely-position these over the map canvas.
