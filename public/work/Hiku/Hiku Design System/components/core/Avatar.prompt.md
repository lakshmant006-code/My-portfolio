Avatar — circular user image with initials fallback.

```jsx
<Avatar src={me.photo} name="Jaden Smith" size={40} ring />
```

Falls back to two-letter initials, then a user glyph, on a soft-green field. `ring` adds the deep-green active outline used on the Account tab.
