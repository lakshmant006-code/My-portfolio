SearchBar — the primary tappable search field on Explore & Trails.

```jsx
<SearchBar title="Nearby" subtitle="Add dates · Add guests" onClick={openSearch} />
```

Rounded (22px) white pill with a leading search icon, a bold location line and a muted dates/guests line. Usually paired with an `IconButton icon="sliders-horizontal"` to its right.
