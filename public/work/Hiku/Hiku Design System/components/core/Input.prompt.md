Input — single-line text field for forms (registration, account).

```jsx
<Input defaultValue="Jaden Smith" />
<Input label="Email" type="email" placeholder="you@asu.edu" />
```

12px radius, hairline border, green focus ring. Optional `label`. Value text is Satoshi Medium.
